#include "helios/serialization/yaml_serializer.h"

// All serialization functions are inline in the header.
// This translation unit ensures the header compiles cleanly and provides
// a place for any future non-inline implementations.
